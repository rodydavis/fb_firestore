name: fb_firestore
description: A Flutter project to use Firebase Cloud Firestore on Mobile and Web
version: 0.0.1+3
author: Rody Davis <rody.davis.jr@gmail.com>
homepage: https://github.com/AppleEducate/fb_firestore
issue_tracker: https://github.com/AppleEducate/plugins/issues
maintainer: Rody Davis (@AppleEducate)

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^0.12.9+5
  firebase: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
